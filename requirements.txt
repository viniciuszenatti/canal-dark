# Canal Dark — Dependências Python (modelo: Shorts narrados)
# Instale com: pip install -r requirements.txt
#
# ATENÇÃO: FFmpeg NÃO está aqui — é um binário do sistema operacional.
# Instale FFmpeg separadamente:
#   Windows : winget install ffmpeg
#   macOS   : brew install ffmpeg
#   Linux   : sudo apt install ffmpeg
# Após instalar, verifique: ffmpeg -version

# TTS grátis sem chave — usa infraestrutura da Microsoft Edge Read Aloud
# Gera .mp3 + timestamps de palavras via SubMaker (eventos SSML boundary)
edge-tts>=7.0  # >=7.0 corrige erro 403 (token Sec-MS-GEC); SRT é montado dos WordBoundary

# Requisições HTTP: download de b-roll do Pexels e chamadas REST gerais
requests==2.32.3

# SDK do Google Gemini para geração de roteiro via gemini-2.5-flash-lite
google-generativeai==0.8.3

# Carregamento de .env em desenvolvimento local (opcional mas recomendado)
python-dotenv==1.0.1

# ── Opcional: ElevenLabs TTS ──────────────────────────────────────────────
# Descomente se quiser usar o engine elevenlabs (--tts-engine elevenlabs).
# Requer ELEVENLABS_API_KEY e plano pago ou créditos grátis.
# elevenlabs==1.3.0

# ── Notas de compatibilidade ─────────────────────────────────────────────
# Python 3.10+ obrigatório (uso de union types com | e asyncio.run)
# Testado em Python 3.11 e 3.12
#
# edge-tts usa asyncio internamente; em Jupyter use:
#   import nest_asyncio; nest_asyncio.apply()  antes de rodar short_factory
